"use client";
import {FormEvent,useEffect,useState} from "react";import {useSearchParams} from "next/navigation";import {getChatRooms,getMessages,PLATFORM_CHANGED_EVENT,sendMessage,type ChatRoom,type Message} from "@/lib/localStorage";import {useAuth} from "./AuthProvider";
export function ChatView(){const {user}=useAuth(),params=useSearchParams(),[rooms,setRooms]=useState<ChatRoom[]>([]),[messages,setMessages]=useState<Message[]>([]),[active,setActive]=useState(params.get("room")||""),[text,setText]=useState("");useEffect(()=>{const sync=()=>{if(!user)return;const visible=getChatRooms().filter(r=>user.role==="admin"||r.customerId===user.id||r.supplierId===user.id);setRooms(visible);setMessages(getMessages());if(!active&&visible[0])setActive(visible[0].id)};sync();window.addEventListener(PLATFORM_CHANGED_EVENT,sync);window.addEventListener("storage",sync);return()=>{window.removeEventListener(PLATFORM_CHANGED_EVENT,sync);window.removeEventListener("storage",sync)}},[user,active]);if(!user)return null;const room=rooms.find(r=>r.id===active),conversation=messages.filter(m=>m.chatRoomId===active);const submit=(e:FormEvent)=>{e.preventDefault();if(room&&user.role!=="admin"){sendMessage(room,user,text);setText("")}};return <div className="container-shell page-pad"><span className="eyebrow">{user.role==="admin"?"Chat Moderation":"Verified Marketplace Chat"}</span><h1 className="mt-3 text-3xl font-black">Chat Inbox</h1><div className="mt-7 grid min-h-[520px] overflow-hidden rounded-2xl border border-line bg-white md:grid-cols-[280px_1fr]"><aside className="border-b border-line md:border-b-0 md:border-r"><h2 className="p-4 font-bold">Conversations</h2>{rooms.map(r=><button key={r.id} onClick={()=>setActive(r.id)} className={`block w-full border-t border-line p-4 text-left ${active===r.id?"bg-primary-soft":""}`}><strong className="text-sm">{user.id===r.customerId?r.supplierName:r.customerName}</strong><p className="mt-1 truncate text-xs text-muted-ink">{r.productTitle||"Order conversation"}</p></button>)}{!rooms.length&&<p className="p-4 text-sm text-muted-ink">No conversations yet.</p>}</aside><section className="flex min-h-[420px] flex-col">{room?<><header className="border-b border-line p-4"><strong>{room.customerName} ↔ {room.supplierName}</strong>{user.role==="admin"&&<p className="text-xs text-muted-ink">Read-only moderation view</p>}</header><div className="flex-1 space-y-3 overflow-y-auto p-5">{conversation.map(m=><div key={m.id} className={`max-w-[80%] rounded-2xl px-4 py-3 text-sm ${m.senderId===user.id?"ml-auto bg-primary text-white":"bg-slate-100"}`}><strong className="block text-xs">{m.senderName}</strong><p className="mt-1">{m.message}</p></div>)}{!conversation.length&&<p className="text-center text-sm text-muted-ink">Start a trusted marketplace conversation.</p>}</div>{user.role!=="admin"&&<form onSubmit={submit} className="flex gap-2 border-t border-line p-4"><input value={text} onChange={e=>setText(e.target.value)} placeholder="Write a message" className="form-control mt-0"/><button className="primary-btn">Send</button></form>}</>:<div className="grid flex-1 place-items-center text-sm text-muted-ink">Select a conversation.</div>}</section></div></div>}
