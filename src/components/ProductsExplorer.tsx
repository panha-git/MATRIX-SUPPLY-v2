"use client";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import { CAMBODIA_PROVINCES, PRODUCT_CATEGORIES } from "@/lib/cambodia";
import { addToCart, createReport, getActiveProducts, PLATFORM_CHANGED_EVENT, startChat, type Product } from "@/lib/localStorage";
import { useMarketplace } from "./MarketplaceProvider";
import { ProductCard } from "./ProductCard";
import { EmptyState, PageHeader } from "./ui";

const reasons=["Misleading product information","Wrong price","Suspicious supplier","Scam or fraud risk","Inappropriate content","Bad image","Other"];
export function ProductsExplorer(){const params=useSearchParams(),router=useRouter(),{requireCustomer,notify}=useMarketplace();const [products,setProducts]=useState<Product[]>([]),[search,setSearch]=useState(params.get("search")||""),[category,setCategory]=useState(params.get("category")||""),[province,setProvince]=useState(""),[sort,setSort]=useState("newest"),[reporting,setReporting]=useState<Product|null>(null),[reason,setReason]=useState(""),[description,setDescription]=useState("");
useEffect(()=>{const sync=()=>setProducts(getActiveProducts());sync();window.addEventListener(PLATFORM_CHANGED_EVENT,sync);window.addEventListener("storage",sync);return()=>{window.removeEventListener(PLATFORM_CHANGED_EVENT,sync);window.removeEventListener("storage",sync)}},[]);
const visible=useMemo(()=>products.filter(p=>(!search||`${p.title} ${p.supplierName}`.toLowerCase().includes(search.toLowerCase()))&&(!category||p.category===category)&&(!province||p.province===province)).sort((a,b)=>sort==="low"?a.price-b.price:sort==="high"?b.price-a.price:new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime()),[products,search,category,province,sort]);
const add=(p:Product)=>requireCustomer(c=>{addToCart(c.id,p);notify(`${p.title} added to your cart`)});const chat=(p:Product)=>requireCustomer(c=>router.push(`/chat?room=${startChat(c,p).id}`));const openReport=(p:Product)=>requireCustomer(()=>setReporting(p));const submitReport=(e:FormEvent)=>{e.preventDefault();if(!reporting||!reason)return;requireCustomer(c=>{createReport({reporterId:c.id,reporterName:c.fullName,targetType:"product",targetId:reporting.id,reason,description});setReporting(null);setReason("");setDescription("");notify("Report submitted to marketplace safety")})};return <div className="container-shell page-pad"><PageHeader eyebrow="Verified Cambodian Marketplace" title="Marketplace" description="Discover products from trusted Cambodian suppliers across every province."/><div className="card mb-7 grid gap-3 p-4 md:grid-cols-[minmax(220px,1fr)_repeat(3,minmax(150px,auto))]"><label className="flex h-11 items-center gap-2 rounded-xl border border-line bg-white px-3"><span className="text-muted-ink">⌕</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search products or suppliers" className="min-w-0 flex-1 bg-transparent text-sm outline-none"/></label><select value={category} onChange={e=>setCategory(e.target.value)} className="h-11 rounded-xl border border-line bg-white px-3 text-sm"><option value="">All categories</option>{PRODUCT_CATEGORIES.map(x=><option key={x}>{x}</option>)}</select><select value={province} onChange={e=>setProvince(e.target.value)} className="h-11 rounded-xl border border-line bg-white px-3 text-sm"><option value="">All provinces</option>{CAMBODIA_PROVINCES.map(x=><option key={x}>{x}</option>)}</select><select value={sort} onChange={e=>setSort(e.target.value)} className="h-11 rounded-xl border border-line bg-white px-3 text-sm"><option value="newest">Newest</option><option value="low">Price: Low to High</option><option value="high">Price: High to Low</option></select></div><div className="mb-4 flex items-center justify-between"><p className="text-sm text-muted-ink"><strong className="text-foreground">{visible.length}</strong> products found</p>{(search||category||province)&&<button onClick={()=>{setSearch("");setCategory("");setProvince("")}} className="text-xs font-bold text-primary">Clear filters</button>}</div>{visible.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{visible.map(p=><ProductCard key={p.id} product={p} onAddToCart={add} onRequestQuote={chat} onChat={chat} onReport={openReport}/>)}</div>:<EmptyState icon="store" title="No products found" description="Try changing your search, category, province, or sorting preference." actionLabel="Clear filters" href="/products"/>}{reporting&&<div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&setReporting(null)}><form onSubmit={submitReport} className="modal-card"><span className="eyebrow">Marketplace Safety</span><h2 className="mt-4 text-2xl font-bold">Report Product</h2><p className="mt-2 text-sm text-muted-ink">Tell us what is concerning about {reporting.title}.</p><label className="mt-5 block text-sm font-semibold">Reason *<select required value={reason} onChange={e=>setReason(e.target.value)} className="form-control"><option value="">Select a reason</option>{reasons.map(x=><option key={x}>{x}</option>)}</select></label><label className="mt-4 block text-sm font-semibold">Description (optional)<textarea rows={4} value={description} onChange={e=>setDescription(e.target.value)} className="form-control py-3"/></label><div className="mt-6 grid grid-cols-2 gap-3"><button type="button" onClick={()=>setReporting(null)} className="secondary-btn">Cancel</button><button className="primary-btn">Submit Report</button></div></form></div>}</div>}
